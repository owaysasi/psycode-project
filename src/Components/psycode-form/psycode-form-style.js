import { makeStyles } from "@mui/styles";

const useStyles = makeStyles({
  psycodeForm: {
    height: "240px",
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "space-between",
  },
  psycodeFormContainer: {
    border: "2px solid rgba(25, 118, 210, 0.5)",
    borderRadius: "12px",
    padding: "50px 200px",
    marginTop: "16px",
  },
});

export default useStyles;
